<UserControl x:Class="FESScript2.Settings.Support.CustomtitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FESScript2.Settings.Support"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="35"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="35"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="999" Background="DarkOrange" MouseLeftButtonDown="Border_MouseLeftButtonDown"/>
        <Ellipse Width="20" Height="20" x:Name="CloseButton" Margin="7.5" Fill="OrangeRed" Grid.Column="4" MouseLeftButtonDown="CloseButton_MouseLeftButtonDown"/>
        <TextBox Background="Transparent" Grid.Column="2" d:Text="SampleText" x:Name="saveText" VerticalAlignment="Center" FontSize="25"  HorizontalAlignment="Center" MouseLeftButtonDown="Border_MouseLeftButtonDown"/>
    </Grid>
</UserControl>
